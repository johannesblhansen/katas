Small project designed to give light examples of spring features, but at the same time being self contained and deployable.

Requirements:
Mysql db - jdbc:mysql://localhost:3306/springbootkata
Gradle

To run:
gradle build && java -jar build/libs/springkata-0.1.0.jar

Asynching feature
http://localhost:8080/asynch